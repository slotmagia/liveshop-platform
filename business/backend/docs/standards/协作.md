# 多人和编码 Agent 协作标准

- 一个任务先登记目标、范围、事实源、不变量、验收和验证级别。
- `module.json`、Proto、migration、组合根和发布脚本在同一任务周期保持单写者。
- 修改共享契约时先改 fragments/Proto 源，再生成产物；禁止直接编辑生成文件。
- 交接必须列出修改文件、契约变化、已运行验证、未决风险和共享文件占用。
- Agent 只读取当前目录链上的 `AGENTS.md` 和任务需要的标准文档，避免用一份超长文档承载全部上下文。
- 发现真正外部兼容阻塞时记录消费者、证据和方案，不自行创建新旧切流或兼容分支。
