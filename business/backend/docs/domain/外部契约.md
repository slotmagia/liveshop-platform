# Platform 外部契约

## 浏览器

Platform Admin contribution 通过 Gateway 调用 Manifest 声明的 HTTP route，并携带 Identity 签发的 Module Capability。Platform 校验 `typ=LIVESHOP-MODULE-CAPABILITY`、Identity issuer、公钥、audience、TTL、module/release/contribution/surface、registry/entitlement/authorization/identity/context revision、permission 与 allowed route，任一不匹配即拒绝。

Platform 不提供浏览器 runtime、有效授权查询或 capability 签发接口。

## 工作负载 gRPC

`liveshop.platform.v1.PlatformRegistryService` 使用 TLS 1.3 双向认证、SPIFFE workload identity 与逐方法权限：

| 方法 | 调用方 | 权限 | 结果 |
|---|---|---|---|
| `GetRouteSnapshot` | Gateway | `platform.registry.routes.read` | revision + 活动 HTTP 路由 |
| `GetActiveCapabilitySnapshot` | Identity | `platform.registry.active-capabilities.read` | revision + 完整结构化活动能力目录 |

活动能力目录包含 module/release/digest/backend、permission definitions、contributions、artifact、frontend contract、navigation、allowed routes、HTTP operations 与 gRPC contract。它不包含用户授权结果；Identity 使用自身事实过滤后才对浏览器暴露 runtime。

`liveshop.platform.v1.PlatformNotificationService` 与 Registry 服务分离，同一 gRPC 端口、同一 SPIFFE 信任表：

| 方法 | 调用方 | 权限 | 结果 |
|---|---|---|---|
| `Dispatch` | 业务模块 workload | `platform.notify-event.dispatch` | 各启用渠道 Delivery |
| `GetDelivery` | 业务模块 workload | `platform.notify-event.dispatch` | 单次渠道投递证据 |

HTTP 内部口 `POST/GET /internal/platform/notify-events/deliveries` 与上述 RPC 共用同一 logic。调用方模块取自 workload subject / SPIFFE，禁止请求体自报模块，浏览器不得调用。

## 发布

Release CI 使用独立 workload identity 注册/激活不可变 Manifest。浏览器角色权限不能替代工作负载权限。

## Admin Live Provider HTTP

| Operation | 方法与路径 | 权限 | 幂等/并发 |
|---|---|---|---|
| `platform.admin.live-providers.drivers.list` | `GET /admin/platform/live-providers/drivers` | `platform.live-provider.read` | safe；返回驱动拥有的表单元数据，不含凭据值 |
| `platform.admin.live-providers.list` | `GET /admin/platform/live-providers` | `platform.live-provider.read` | safe；只返回脱敏凭据状态 |
| `platform.admin.live-providers.update` | `PUT /admin/platform/live-providers/{code}` | `platform.live-provider.manage` | `commandKey + expectedVersion` |
| `platform.admin.live-providers.retire` | `POST /admin/platform/live-providers/{code}/retire` | `platform.live-provider.manage` | `commandKey + expectedVersion` |

浏览器不能提交商户/店铺范围；Admin Module Capability 不携带店铺上下文，claims 也不再包含 `appId`/`commercialId`，因此本接口写入单全局 Provider 目录。Admin 必须按驱动元数据生成字段，不能自行维护另一份驱动条件表单。所有驱动表单必须显式提交 `enabled`，创建默认值为 `true`；缺少该参数的写请求直接拒绝，不能按 Go 布尔零值解释为停用。密钥变更显式使用 `KEEP / REPLACE / CLEAR`；读取不返回明文。物理 DELETE 不公开。

## 边缘 Ask / Route / Snapshot

详见 [`edge/外部契约.md`](./edge/外部契约.md)。Caddy 与 Gateway 调用 classic HTTP；Identity 读取 JSON 快照拿 CNAME。都使用内部 grant，不进入浏览器 Manifest。

## Admin 通知事件 HTTP

| Operation | 方法与路径 | 权限 |
|---|---|---|
| `platform.admin.notify-events.list` | `GET /admin/platform/notify-events` | `platform.notify-event.read` |
| `platform.admin.notify-events.get` | `GET /admin/platform/notify-events/{eventKey}` | `platform.notify-event.read` |
| `platform.admin.notify-events.policy.replace` | `PUT /admin/platform/notify-events/{eventKey}/policy` | `platform.notify-event.manage` |
| `platform.admin.notify-events.deliveries.list` | `GET /admin/platform/notify-events/{eventKey}/deliveries` | `platform.notify-event.read` |
| `platform.admin.notify-templates.list` | `GET /admin/platform/notify-templates` | `platform.notify-template.read` |
| `platform.admin.notify-templates.get` | `GET /admin/platform/notify-templates/{code}` | `platform.notify-template.read` |
| `platform.admin.notify-templates.update` | `PUT /admin/platform/notify-templates/{code}` | `platform.notify-template.manage` |
| `platform.admin.notify-templates.retire` | `POST /admin/platform/notify-templates/{code}/retire` | `platform.notify-template.manage` |
| `platform.admin.notify-channels.in-app.get` | `GET /admin/platform/notify-channels/in-app` | `platform.notify-channel.read` |
| `platform.admin.notify-channels.in-app.update` | `PUT /admin/platform/notify-channels/in-app` | `platform.notify-channel.manage` |

短信/邮件 HTTP 仍用 `platform.admin.sms*` / `platform.admin.email*` 与 `/admin/platform/sms*` `/admin/platform/email*`，由「通知方式」页复用，不另建第三套事实。测试发送不创建业务 Delivery，也不走 `eventKey`。事件目录来自 Registry 投影，Admin 不能新建 `eventKey`。

## Admin / Shop 埋点 HTTP

| Operation | 方法与路径 | 权限 |
|---|---|---|
| `platform.admin.track-events.list` | `GET /admin/platform/track-events` | `platform.track-event.read` |
| `platform.shop.track-events.create` | `POST /shop/platform/track-events` | `platform.track-event.write` |
| `platform.shop.health.get` | `GET /shop/platform/health` | 无（public） |

Shop 写入鉴权为 Identity Module Capability（`module-session`，surface=`shop`）。租户与主体只取 claims；响应返回 `accepted` / `duplicates` / `rejected` 与逐行 `errors`。浏览器不得携带 `appId` / `commercialId`。Admin 页只调用 list，详情用 list 行字段经 `hostFormModal` 只读展示。

## 授权数据迁移

Platform export、Identity IAM receipt 与 Subscription entitlement receipt 契约见 [`授权交接.md`](../授权交接.md)。这是一次性交接，不是运行时 API，也不存在双读、双写或 fallback。
