# Platform 事实源

| 业务事实 | 唯一事实源 | 消费者 |
|---|---|---|
| 模块 release Manifest 与 digest | Platform Registry MySQL | Release CI、Admin |
| 模块当前活动版本 | Platform Registry MySQL | Gateway、Identity、Admin |
| Registry revision | Platform Registry MySQL | Gateway、Identity |
| 活动路由快照 | 活动 release 派生 | Gateway |
| 活动权限定义、贡献、HTTP/gRPC/前端契约快照 | 活动 release 派生 | Identity |
| Platform 设置 | Platform Settings MySQL | Admin |
| Registry/Settings 操作审计 | Platform Audit MySQL | Admin |
| 短信/邮件 Provider、模板、通知事件、投递与尝试证据 | Platform Notification MySQL | Identity、Catalog、Trade、Live、Admin |
| 对象存储驱动、通道、密钥引用与健康状态 | Platform Storage MySQL | Catalog、Live、Admin |
| 全局埋点原始事件、接收结果、水位与广告归因投影 | Platform Telemetry MySQL | Admin、业务只读消费者 |
| 直播 Provider、商户/店铺分配、凭据引用与健康状态 | Platform Live Provider MySQL | Live、Admin |
| 语言、翻译版本与发布状态 | Platform Localization MySQL | 四端 Host、业务模块、Admin |
| 用户、组织、角色、策略、DataScope、有效授权 | Identity | Host、业务模块 |
| 游客风险决策、客服账号、商户能力治理 | Identity | Trade、Live、Admin |
| 素材资产与生命周期 | Catalog | 商品、Live |
| 经营分析、店铺访问与广告商品映射 | Catalog | Merchant、Admin、Platform attribution |
| 直播房间、场次和直播专属指标 | Live | Admin、Merchant、Shop、主播端 |
| Module Capability | Identity 签名 | Gateway、Platform、业务模块 |

旧 Platform 授权表只允许作为一次性迁移输入；完成 Identity 持久导入回执校验并 finalize 后删除，不能再次成为运行时事实源。

店铺隔离是 `merchant_id + shop_id`。`app_id`/`commercial_id` 不是运行时标识，也不进入 Module Capability claims。Provider / SMS / Email / Storage 目录是无 `app_id` 的单全局目录。

## Live Provider 当前事实

- `DriverDefinition` 注册表拥有驱动 code、kind、推流形态、表单字段、默认值、枚举、凭据分组和校验边界；Admin 只解释该元数据。
- `live_provider` 是当前 Provider 目录；总后台维护单全局目录，业务键为 `code`，`code` 创建后不可变。
- `live_provider_version` 是 INSERT-only 的不可变版本快照，包含该版本运行所需非敏感配置与加密凭据。
- `live_provider_catalogue.revision` 是 Provider 目录写入修订号，也是并发命令的聚合锁；目录是 `id=1` 的单例。
- `live_provider_command` 是稳定 `commandKey` 的幂等回执；相同 key 不同请求摘要必须冲突。
- `platform_audit_event` 与 Provider 当前状态、版本快照和命令回执在同一事务提交。
- `health_status` 初始为 `UNKNOWN`；没有真实探测证据时页面显示“未检查”，不得伪造健康。
