// Package application implements {{MODULE_ID}} use cases.
package application

import (
	"context"

	"{{GO_MODULE}}/backend/internal/{{MODULE_PACKAGE}}/domain"
)

type Service struct{ repository domain.Repository }

func New(repository domain.Repository) *Service { return &Service{repository: repository} }

func (s *Service) Health(ctx context.Context) error { return s.repository.Ready(ctx) }
