// Package domain owns {{MODULE_ID}} business facts and invariants.
package domain

import "context"

type Status string

const StatusActive Status = "ACTIVE"

type Repository interface {
	Ready(context.Context) error
}
