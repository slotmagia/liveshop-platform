# {{MODULE_NAME}}

`{{MODULE_ID}}` 是由 LiveShop business-module 模板创建的独立业务模块。

开始开发前完成 `backend/docs/domain/` 下的事实、不变量、状态机、事务和外部契约，并将示例 API、Proto 和 migration 替换为真实业务定义。

```powershell
./backend/tools/verify-fast.ps1
```
